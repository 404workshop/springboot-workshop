# Copy this file to .env and set real values (never commit .env)
# Production PostgreSQL credentials - externalized via environment variables
POSTGRES_URL=jdbc:postgresql://localhost:5432/ecommerce_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Active profile
SPRING_PROFILES_ACTIVE=production
